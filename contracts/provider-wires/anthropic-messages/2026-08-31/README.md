# anthropic-messages wire — 2026-08-31

This immutable cut pins exact compact JSON request bytes, all non-secret header values for each
credential presentation, `message_stop` as the terminal event with no sentinel, and the complete
event and content-delta inventories interpreted by production. Fixtures are synthetic and the
evidence class is `provider_emulated`.
