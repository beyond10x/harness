[package]
name = "b10x-harness-wire"
description = "Neutral values and ports shared by the b10x agent loop and its model wires"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
# Parses the standard HTTP-date alternative in Retry-After. The function receives both headers
# and clock as values, so the wire crate still performs no I/O and reads no ambient time.
httpdate = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
zeroize = { workspace = true }
