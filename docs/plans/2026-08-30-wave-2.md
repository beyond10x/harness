# Wave 2 — 2026-08-30

Proposed, not approved. Nothing runs until the operator says so.

Base: `main` at `c5bb2ed`, one commit ahead of `origin/main` (`4f7c633`).
Integration branch: `wt/integrate-2`. One worktree and one build directory per unit.

## The base gate is red

`bash scripts/gate.sh` at `c5bb2ed` **fails**, twice out of two runs, on one test:

```
---- an_interrupt_queued_before_the_turn_starts_does_not_crash_the_server stdout ----
panicked at crates/harness-cli/tests/bridge_mode.rs:706:5:
assertion `left == right` failed: the server must survive and report, not die
test result: FAILED. 16 passed; 1 failed
```

The same suite run alone passes **5/5** (`cargo test -p b10x-harness-cli --test bridge_mode -- --test-threads=1`).
With that one test skipped the whole gate is green: **925 tests passed, 0 failed**, and all six gate
steps exit `0`.

This is the race `story:bridge-interrupt-race-pinned` was opened for, on the *adjacent* test — the
story names `an_unknown_request_mid_turn_is_refused_without_killing_the_server`, and the one that
actually fails today is its sibling four lines above it. Both call `Endpoint::start("slow")` and
neither gates on a streamed event before writing the interrupt.

**Consequence:** a wave cannot be closed on one gate run until this is fixed, because the gate's
verdict at the merge is not a fact about the merge. U5 is therefore first in and first merged.

## Pre-flight, measured

| Fact | Number |
|---|---|
| Working tree | clean, on `main` |
| Existing worktrees | none (`git worktree list` = 1 row, the main checkout) |
| Stale build directories | none found |
| Free disk | **75 GB**, filesystem at 91% |
| One cold worktree build (`cargo test --workspace --locked --no-run`) | **18 s**, **1.2 GB** |
| Same worktree after the tests have run | **2.3 GB** |
| Full gate, warm | **39 s** |
| sccache | wired (`~/.cargo/config.toml`), 173 Rust misses on the cold build, so it is filling |
| Cores | 20 |
| **Six worktrees cost** | **≈ 14 GB of 75 GB free** |

`main/target` is **15 GB**, but that is accumulated incremental state, not the cost of one build.
Disk does not bound N here. **N = 6.**

## The units

| # | Story | Serves, as stored | Package | Files it owns |
|---|---|---|---|---|
| U1 | `story:changelog-in-the-same-change` | `epic:tracking-documents-current` → `initiative:record-matches-the-code` → `vision:b10x-owns-its-loop` | — | `CHANGELOG.md` |
| U2 | `story:status-page-contradicts-the-code` | same chain as U1 | — | `STATUS.md` |
| U3 | `story:no-home-path-reaches-a-commit` | `epic:adoption-follow-ups` — **chain stops there** | — | `scripts/check-no-home-paths.py` (new), `scripts/gate.sh`, `AGENTS.md`, `README.md` gate table |
| U4 | `story:cli-contract-dated-ahead` | `epic:pinned-interfaces-honest` → `initiative:record-matches-the-code` → `vision:b10x-owns-its-loop` | `b10x-harness-cli` | `contracts/cli/b10x-harness/`, `crates/harness-cli/src/contract.rs` |
| U5 | `story:bridge-interrupt-race-pinned` | `epic:gate-stays-trustworthy` → `initiative:record-matches-the-code` → `vision:b10x-owns-its-loop` | `b10x-harness-cli` | `crates/harness-cli/tests/bridge_mode.rs`, the `slow` fixtures |
| U6 | `story:verbs-surface-narrowing` | `epic:adoption-follow-ups` — **chain stops there** | `b10x-harness-loop` | `crates/harness-loop/src/{lib.rs,agent.rs,tests.rs}` |

`epic:adoption-follow-ups` carries no `decomposes` edge — it is still an unfilled `protocol artifact
new` template — so U3 and U6 reach no initiative and no vision in the store. The lifecycle
(`artifacts/lifecycles/story.yaml`) does **not** require one: the only stated requirement is
`evidence: test_result, at_least: 1` for `implemented`. Nothing is therefore blocked, but two of the
six units cannot be traced to an objective, and the honest reading of `AGENTS.md` § *Serves* is that
they belong to **O1 — governed reach** (U6, an unenforced narrowing) and to the gate (U3). That
mapping is **inferred, not stored**; the edge should be added to the epic at close.

U6 is the only unit with a safety consequence: **a named agent granted `[Read, Grep]` can reach
`file_write` today under `--surface verbs`, because the guard sees the call named `tool_invoke` and
the entry is only an argument** (`crates/harness-loop/src/lib.rs:3517-3538`, the guard's own comment
says so). Under `--surface flat` the same agent is correctly refused. That is a narrowing that
silently does not narrow.

## Overlap, per pair

Every pair, from the scoped surfaces. `—` means no shared file.

|  | U2 | U3 | U4 | U5 | U6 |
|---|---|---|---|---|---|
| **U1** | — | — | **`CHANGELOG.md`** | — | **`CHANGELOG.md`** |
| **U2** | | — | — | — | — |
| **U3** | | | — | — | — |
| **U4** | | | | same package, different files | — |
| **U5** | | | | | — |

Two real collisions, both on `CHANGELOG.md`, both against U1.

**Resolution: the orchestrator owns `CHANGELOG.md`.** U4 and U6 are forbidden to edit it and instead
return their entry text in their report; U1 is merged first and the two entries are written onto the
integration branch by the coordinator. Invariant 14 ("re-pins the fixture *and* enters the
changelog") is satisfied in the integration commit, not in the unit commit — stated here rather than
discovered at merge.

U4 and U5 share the package `b10x-harness-cli` but no file (`src/contract.rs` vs
`tests/bridge_mode.rs`). **U6 must not change the clap surface**: any new flag re-pins `argv.json`
and collides with U4. If U6 needs one, it leaves the wave.

## Deliberately left out

| Story | Why |
|---|---|
| `story:argv-pin-carries-effective-defaults` | Overlaps U4 heavily — same version directory, same `ARGV_CONTRACT_VERSION`. The scoper's verdict: "they cannot run in the same wave in either order". Next wave. |
| `compaction-measured-live`, `flat-surface-measured`, `messages-cache-breakpoint-live-pin`, `responses-pin-from-live-bytes`, `oauth-token-renewal`, `chatgpt`-route work | Each needs a paid live provider run or a subscription credential. Not implementable in this tree tonight. |
| `vllm-reasoning-events-unpinned` | Acceptance needs a run against vLLM. `third-party-blocker:runpod-account-unfunded` is open: RunPod returns 500 "account balance too low" on every pod create. The code change is small; the evidence cannot be produced. |
| `real-bridge-drives-this-binary`, `direct-provider-adapter`, `one-conformance-suite-over-three-workspaces`, `workflow-run-through-the-library` | Need a second harness, a consumer's adapter, or real confinement on a delegated cgroup scope. |
| `daemon-exec-start-adoption`, `repo-local-profiles` | Each carries an open question for the operator inside the story — a trust decision and an id-vocabulary decision. Not an implementor's call. |
| `embedded-exec-exercised` | Needs a delegated cgroup scope on a real machine. Blast radius unknown; unassessed rather than safe. |

## Store finding: the last wave did not close

`story:a-steps-scope-is-the-scope-it-runs-under` and `story:section-sessions-name-every-attempt` are
both `draft`, and both are **already implemented on `main`**:

| Story | Commit |
|---|---|
| `a-steps-scope-is-the-scope-it-runs-under` | `4419377 feat(workflow): a step runs under the write scope its node declares` |
| `section-sessions-name-every-attempt` | `c4034ec fix(workflow): a section's session names every attempt above it` |

merged through `wt/step-scope` and `wt/section-sessions` into `wt/integrate`, released as `0.3.0`
(`4f7c633`). The previous wave shipped the code and skipped *Close it* step 3.

Neither can go to `implemented`: the store requires ≥1 `test_result` evidence
(`artifacts/lifecycles/story.yaml`), and both stories' own acceptance names a re-run of metaharness
`run-native.sh` — a paid native walk — as the proof. **Default taken on silence: move both to
`active`** (work in flight, evidence outstanding), which is true of the tree and is not a claim
nothing checked.

Their `wt/*` branches are left standing; the worktrees are already gone.

## What each implementor is told

- Its story id and acceptance statement.
- It writes source and tests only. **Nothing under `.engineering/planning/`. Nothing in `CHANGELOG.md`.**
- It gates package-scoped (`cargo test -p <pkg> --locked`); the whole gate runs once, later, on the
  integration branch.
- Test first; the red run goes in its report verbatim.
- Its own worktree and its own `CARGO_TARGET_DIR`, both named after the unit.

## Routing

Green → merge into `wt/integrate-2`. Red once → same implementor with the adversary's findings. Red
twice → a fresh implementor with the findings, the diff and what was tried. Red three times → the
unit leaves the wave and the reason is recorded.

U5 merges first, because until it lands the gate cannot decide anything.

## Close

Whole gate once on `wt/integrate-2`, reading `scripts/gate.sh`'s own exit status and not a
pipeline's. Evidence recorded per story against the merge commit, each story moved through
`protocol artifact move`, `protocol artifact validate` relayed verbatim. Then the worktrees and every
named build directory come down, and `git worktree list` is read back.

**Release is not part of this wave.** It stops for a person.
