# Review: design 0002 wave (sub-agents, structured output, hooks) — uncommitted tree, 2026-08-29

Scope: the uncommitted diff (53 files, +16,253/−728) with the 0002 surface read in full:
`crates/harness-loop/src/{lib,answer,delegate,hook,event}.rs`, `crates/harness-cli/src/{hooks,render,lib}.rs`,
the design, README and CHANGELOG. Gate at the time of reading: `bash scripts/gate.sh` exit 0, 687 passed / 0 failed / 1 ignored.

Verdict: **no blocker; 1 Medium, 5 Low, 2 informational.** Nothing here is a safety-envelope hole; the Medium is a
pre-loop refusal that is missing.

## Findings

| # | sev | where | defect | consequence for a person | fix |
|---|---|---|---|---|---|
| 1 | Medium | `crates/harness-cli/src/lib.rs:646` (no range on `--delegate-turns`); `crates/harness-loop/src/budget.rs:73-75` (zero refused); `crates/harness-loop/src/lib.rs` `delegate()` `Err` arm | `--delegate-turns 0` is accepted by clap and refused only inside the child's `Budget::validate`, after `DelegateStarted` | an operator typo turns **every** delegation into a failed tool result (`the delegate could not run: … max_turns …`), each one after a paid parent turn — contradicting the repo's own "refuse before the first request" rule that the parent's `--max-turns 0` follows | `#[arg(…, value_parser = clap::value_parser!(u64).range(1..))]`; one unit test; re-pin `argv.json` if the checker records value parsers. **Verified at runtime**: `b10x-harness run --model m --delegate --delegate-turns 0 …` emitted `{"kind":"started",…,"published_tools":[…,"delegate"]}` |
| 2 | Low | `crates/harness-loop/src/lib.rs:2085-2089` (position scan) and `:2143-2170` (`run_owned` returns `None` for non-object args, arg bound, before-call block) | every other call of a turn that carries `answer` is refused with the fixed sentence *the run ended with the answer* **before** the answer's own outcome is known; when the answer then fails, the run continues | the model reads *the run ended* for its `file_write` and *call it again* for `answer` in the same turn — contradictory; it may redo the write or stop | reword `ANSWER_ENDED` to what is true on both paths: *refused: made in the same turn as `answer`, which must be called alone*; add a test for `[file_write, answer(non-object)]` (no test covers a failed answer beside siblings — `tests.rs:3446`, `:3546` cover the two halves separately) |
| 3 | Low | `crates/harness-loop/src/lib.rs:2717-2721`; `crates/harness-cli/src/hooks.rs` `after_call` folds `Block`/`Failed` into notes | `HookRan { point: after-call }` always carries `decision: proceed`, even when the hook crashed | a reader of the `--json` record cannot see that an after-call hook failed; only the model sees it, inside `hook_notes` | either document that after-call failures live in the result, or let `HookPort::after_call` return `(Option<String>, HookDecision)` and record the decision. Recommendation: document (M-later) |
| 4 | Low | `README.md` § *Sub-agents, structured output, hooks* row 1; `CHANGELOG.md:14`; `crates/harness-cli/src/lib.rs:680-688` (`--output-schema` doc); `crates/harness-cli/src/render.rs:127-133` | "Stdout is that JSON and nothing else" is false under `--json`: stdout is the JSONL record and the answer is the **latest `answered` event before a `finished` with `stop: completed`** — and a stop-hook withdrawal leaves an earlier `answered` in the record | a driver combining `--json --output-schema` (the metaharness shape) finds no bare JSON line; one that takes the *first* `answered` takes a withdrawn one | one sentence in the three places; e2e test `--json --output-schema` asserting the last `answered`/`finished` pair |
| 5 | Low | `crates/harness-loop/src/lib.rs` `answer_or_nudge` (`state.nudged` never reset); `stop_hook` clears `structured` on a block | one nudge per **run**, not per ending: prose → nudge → `answer` → stop hook blocks → model ends in prose → `Unstructured` with no second nudge | a run that answered, was told by the operator's hook to go on, and then replied in prose exits 2 with empty stdout although one more nudge would almost certainly have produced the answer | reset `state.nudged = 0` where `stop_continues` is incremented; one test |
| 6 | Low | `crates/harness-loop/src/event.rs:251` (`events_round_trip` covers `ApprovalRequired` only) | the five new variants (`Answered`, `DelegateStarted`, `DelegateFinished`, `Delegated{Box<LoopEvent>}`, `HookRan`) have no deserialisation test | none today — no Rust code outside `event.rs` deserialises `LoopEvent` (grep) — but the JSONL record is a published interface (README: metaharness reads it) | extend `events_round_trip` with a nested `Delegated{DelegateFinished}` and a `HookRan{Block}` |
| 7 | info | `crates/harness-cli/src/hooks.rs:56-63` | hooks are bounded by `HOOK_TIMEOUT` (60 s) per hook, sequential per point, **not** by the run's `--max-duration-ms` | a run with three before-call hooks can overshoot its deadline by up to 180 s per call | documented; no change |
| 8 | info | `crates/harness-loop/src/lib.rs:2565-2650` (`invoke` returns before any hook for unpublished, arg-bound and approver-denied calls) | `after-call` does not fire for refused calls; design § 3 says "after the outcome, before it is recorded" | an audit hook misses refusals (they are still in `ToolCompleted{failed}` / `ApprovalResolved`) | decide: document as intended (recommended) or fire after-call on refusals |

## Verified sound (read end to end, with the test that pins each)

| property | evidence |
|---|---|
| child spend absorbed on every exit path, before branching on how it ended | `lib.rs` `delegate()`: `state.absorb_child(&mut child_state)` precedes the `match ran`; `tests.rs:3990`, `:4047` |
| cancel token shared; a cancelled child ends the parent at the next check | `tests.rs:4090` |
| hooks fire on owned calls; before-call after the approver; fail closed before a call, fail open at stop | `lib.rs` `run_owned`, `invoke`, `stop_hook`; `tests.rs:2929`, `:2968`, `:3031`, `:3216`, `:4354` |
| stop hook not consulted at a delegate's end | `lib.rs` `stop_hook` `if self.nested`; `tests.rs:4463` |
| owned names refused pre-loop, including verb-surface shadowing via `ToolPort::invoked` | `lib.rs` `check_owned_names`; `tests.rs:3632`, `:3684` |
| credential env var stripped from hooks — only when named (`api_key_env: Option<String>`, no default, `lib.rs:355`) | `hooks.rs` `without_env`; `end_to_end.rs:1275-1301`; `hooks.rs` test `a_named_variable_is_removed…` |
| hook drains bounded by `recv_timeout`; stdout over 16 KiB refused, stderr cut labelled; grandchild on the pipe abandoned at the bound | `hooks.rs` `collect`, `bounded`, `Drained::cut`; tests `…grandchild…`, `…cut…`, `…refused_rather_than_cut` |
| renderer: answer written once at `Finished`, only when completed; no bare newline on a retried turn under a schema | `render.rs` tests `an_answer_a_stop_hook_withdrew_is_never_printed`, `a_retried_turn_puts_no_bare_newline…` |
| a run with hooks batches nothing; owned calls never batched | `lib.rs` `batchable`; `tests.rs:3239`, `:4169` |
| `harness-wire` untouched by 0002; app-server ignores the new events and maps `Unstructured` to `failed` | `git status` (wire diff is the earlier wave's envelope/port change); `session.rs:220-224`, `lib.rs:476` |

## Not reviewed here

The earlier wave's changes in the same tree (`harness-tools` `flat.rs`/`search`/`find`, `harness-substrate`, provider wires,
`approve.rs`, `environment.rs`, `contract.rs`) were reviewed in the SOTA-comparison pass and are unchanged since.

## Outcome (same day)

| # | outcome | where |
|---|---|---|
| 1 | fixed — `--delegate-turns` takes `range(1..)`; zero is a clap error (exit 2) | `crates/harness-cli/src/lib.rs` (`a_delegate_that_may_take_no_turn_at_all_is_a_parse_error_naming_the_flag`) |
| 2 | fixed — the sibling refusal reads *made in the same turn as `answer`, which must be called alone* | `crates/harness-loop/src/lib.rs` (`a_failed_answer_beside_other_calls_refuses_them_without_claiming_the_run_ended`) |
| 3 | fixed — `HookPort::after_call` returns `AfterCall { note, decision }`; a failed hook is `HookRan{decision: failed}` | `crates/harness-loop/src/hook.rs`, `lib.rs`, `crates/harness-cli/src/hooks.rs` |
| 4 | fixed — README, flag doc, design and CHANGELOG state the `--json` rule; two e2e tests pin it | `crates/harness-cli/tests/end_to_end.rs` (`under_json_the_answer_is_the_last_answered_event…`, `a_withdrawn_answer_stays_in_the_json_record…`) |
| 5 | fixed — `nudged` resets when a stop hook continues the run | `crates/harness-loop/src/lib.rs` (`a_stop_hook_continuation_earns_the_run_a_fresh_nudge…`) |
| 6 | fixed — nested `Delegated{DelegateFinished}`, `HookRan`, `Answered`, `DelegateStarted` round-trip | `crates/harness-loop/src/event.rs` |
| 7 | no change (documented) | `crates/harness-cli/src/hooks.rs:56-63` |
| 8 | documented as intended | `crates/harness-loop/src/hook.rs`, design § 3 |

Gate after the fixes: `bash scripts/gate.sh` exit 0, **696 passed / 0 failed / 1 ignored** (loop 146, cli 123 + 36 e2e + 17 bridge).
